{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Stores user profile information including XP, streaks, and preferences.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user profile."
        },
        "userId": {
          "type": "string",
          "description": "User ID from the authentication system (e.g., Firebase)."
        },
        "xp": {
          "type": "number",
          "description": "User's experience points."
        },
        "streak": {
          "type": "number",
          "description": "User's current daily streak."
        },
        "favoriteGames": {
          "type": "array",
          "description": "References to the user's favorite games.",
          "items": {
            "type": "string"
          }
        },
        "themePreference": {
          "type": "string",
          "description": "User's preferred theme (e.g., 'light', 'dark')."
        }
      },
      "required": [
        "id",
        "userId",
        "xp",
        "streak"
      ]
    },
    "Microgame": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Microgame",
      "type": "object",
      "description": "Stores information about individual microgames.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the microgame."
        },
        "name": {
          "type": "string",
          "description": "Name of the microgame."
        },
        "description": {
          "type": "string",
          "description": "Description of the microgame."
        }
      },
      "required": [
        "id",
        "name",
        "description"
      ]
    },
    "PlayHistory": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "PlayHistory",
      "type": "object",
      "description": "Stores the user's play history for each microgame.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the play history entry."
        },
        "userId": {
          "type": "string",
          "description": "Reference to UserProfile. (Relationship: UserProfile 1:N PlayHistory)"
        },
        "microgameId": {
          "type": "string",
          "description": "Reference to Microgame. (Relationship: Microgame 1:N PlayHistory)"
        },
        "timestamp": {
          "type": "string",
          "description": "Timestamp of when the game was played.",
          "format": "date-time"
        },
        "score": {
          "type": "number",
          "description": "User's score in the microgame."
        }
      },
      "required": [
        "id",
        "userId",
        "microgameId",
        "timestamp"
      ]
    },
    "DailyChallenge": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "DailyChallenge",
      "type": "object",
      "description": "Stores the AI-generated daily challenge for a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the daily challenge."
        },
        "userId": {
          "type": "string",
          "description": "Reference to UserProfile. (Relationship: UserProfile 1:N DailyChallenge)"
        },
        "date": {
          "type": "string",
          "description": "Date for which the challenge is generated.",
          "format": "date"
        },
        "gameId": {
          "type": "string",
          "description": "Reference to Microgame to be suggested for the day"
        },
        "challengeText": {
          "type": "string",
          "description": "Textual description of the daily challenge."
        }
      },
      "required": [
        "id",
        "userId",
        "date",
        "challengeText"
      ]
    }
  },
  "auth": {
    "providers": [
      "google.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}/userProfile",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores user profile information. Path-based ownership ensures only the user can access their profile.",
          "params": [
            {
              "name": "userId",
              "description": "The unique ID of the user."
            }
          ]
        }
      },
      {
        "path": "/microgames/{microgameId}",
        "definition": {
          "entityName": "Microgame",
          "schema": {
            "$ref": "#/backend/entities/Microgame"
          },
          "description": "Stores information about available microgames.",
          "params": [
            {
              "name": "microgameId",
              "description": "The unique ID of the microgame."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/playHistory/{playHistoryId}",
        "definition": {
          "entityName": "PlayHistory",
          "schema": {
            "$ref": "#/backend/entities/PlayHistory"
          },
          "description": "Stores the play history for each user. Path-based ownership enforced.",
          "params": [
            {
              "name": "userId",
              "description": "The unique ID of the user."
            },
            {
              "name": "playHistoryId",
              "description": "The unique ID of the play history entry."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/dailyChallenges/{dailyChallengeId}",
        "definition": {
          "entityName": "DailyChallenge",
          "schema": {
            "$ref": "#/backend/entities/DailyChallenge"
          },
          "description": "Stores the daily challenges for each user. Path-based ownership enforced.",
          "params": [
            {
              "name": "userId",
              "description": "The unique ID of the user."
            },
            {
              "name": "dailyChallengeId",
              "description": "The unique ID of the daily challenge."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to address the 'auth/operation-not-allowed' error, which typically indicates that Firebase Authentication methods are not enabled in the Firebase Console. While the database structure doesn't directly fix this, it ensures a scalable and secure data model that can support various authentication scenarios after enabling the necessary methods (like Google Sign-In). It focuses on Authorization Independence, enabling clear and debuggable security rules. This is achieved by using path-based ownership for user-specific data and avoiding `get()` calls in security rules. Each collection is designed to have a homogeneous security posture, simplifying rules.\n\nUser profiles are stored in `/users/{userId}/userProfile` for path-based ownership. User-specific data like play history and daily challenges are stored as subcollections of the `/users/{userId}` collection. Microgames are stored in the `/microgames` collection. This design facilitates QAPs by enabling secure `list` operations based on the user's `userId` without relying on complex filtering in rules."
  }
}